{
  "name": "cv-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file offline tool: read Bilin's CV, diff it against the site data, emit updated data files.",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "test": "node test/run-test-core.js && node test/run-test-docx.js"
  },
  "devDependencies": {
    "jszip": "^3.10.1",
    "pdfjs-dist": "3.11.174"
  }
}
